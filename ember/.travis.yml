---
language: node_js
node_js:
  - "10"
dist: xenial
addons:
  chrome: stable
cache:
  yarn: true
env:
  global:
    - JOBS=1
branches:
  only:
    - master
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash
  - export PATH=$HOME/.yarn/bin:$PATH
script:
  - yarn test
